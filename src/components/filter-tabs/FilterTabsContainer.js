import styled from "styled-components";
import { primary } from "../../variables/ColorVariables";

export const FilterTabsContainer = styled.div`
  border-bottom: 2px solid ${primary};
  display: flex;
  justify-content: space-evenly;
`;
